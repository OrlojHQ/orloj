components:
  schemas:
    ToolRetryPolicy:
      type: object
      properties:
        max_attempts: { type: integer }
        backoff: { type: string }
        max_backoff: { type: string }
        jitter: { type: string }
    ToolRuntimePolicy:
      type: object
      properties:
        timeout: { type: string }
        isolation_mode: { type: string }
        retry: { $ref: "#/components/schemas/ToolRetryPolicy" }
    ToolAuth:
      type: object
      properties:
        profile: { type: string }
        secretRef: { type: string }
        headerName: { type: string }
        tokenURL: { type: string }
        scopes:
          type: array
          items: { type: string }
    ToolCliEnvRef:
      type: object
      required: [name, secretRef]
      properties:
        name: { type: string }
        secretRef: { type: string }
        key: { type: string }
    ToolCliSpec:
      type: object
      properties:
        command: { type: string }
        args:
          type: array
          items: { type: string }
        image: { type: string }
        image_pull_secret: { type: string, description: "Name of a Secret containing registry credentials for pulling cli.image." }
        network: { type: string }
        stdin_from_input: { type: boolean }
        output: { type: string }
        working_dir: { type: string }
        env:
          type: object
          additionalProperties: { type: string }
        env_from:
          type: array
          items: { $ref: "#/components/schemas/ToolCliEnvRef" }
    ToolSpec:
      type: object
      properties:
        type: { type: string, enum: [http, external, grpc, webhook-callback, mcp, wasm, cli] }
        endpoint: { type: string }
        description: { type: string }
        input_schema: { type: object, additionalProperties: true }
        mcp_server_ref: { type: string }
        mcp_tool_name: { type: string }
        cli: { $ref: "#/components/schemas/ToolCliSpec" }
        capabilities:
          type: array
          items: { type: string }
        operation_classes:
          type: array
          items: { type: string }
        risk_level: { type: string }
        runtime: { $ref: "#/components/schemas/ToolRuntimePolicy" }
        auth: { $ref: "#/components/schemas/ToolAuth" }
    ToolStatus:
      type: object
      properties:
        phase: { type: string }
        lastError: { type: string }
        observedGeneration: { type: integer, format: int64 }
    Tool:
      type: object
      required: [apiVersion, kind, metadata, spec]
      properties:
        apiVersion: { type: string, enum: [orloj.dev/v1] }
        kind: { type: string, enum: [Tool] }
        metadata: { $ref: "common.yaml#/components/schemas/ObjectMeta" }
        spec: { $ref: "#/components/schemas/ToolSpec" }
        status: { $ref: "#/components/schemas/ToolStatus" }
    ToolList:
      allOf:
        - { $ref: "common.yaml#/components/schemas/ListMeta" }
        - type: object
          required: [items]
          properties:
            items:
              type: array
              items: { $ref: "#/components/schemas/Tool" }
