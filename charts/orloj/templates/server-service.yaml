apiVersion: v1
kind: Service
metadata:
  name: {{ include "orloj.fullname" . }}-server
  labels:
    {{- include "orloj.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.server.service.type "NodePort") .Values.server.service.nodePort }}
      nodePort: {{ .Values.server.service.nodePort }}
      {{- end }}
    {{- if .Values.a2a.grpc.enabled }}
    - port: {{ .Values.a2a.grpc.port }}
      targetPort: a2a-grpc
      protocol: TCP
      name: a2a-grpc
    {{- end }}
  selector:
    {{- include "orloj.server.selectorLabels" . | nindent 4 }}
