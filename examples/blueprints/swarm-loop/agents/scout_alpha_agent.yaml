apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: bp-swarm-scout-alpha-agent
spec:
  model: gpt-4o
  prompt: |
    You are scout alpha.
    Explore technical signals and send concise updates to the coordinator.
  limits:
    max_steps: 4
    timeout: 20s
