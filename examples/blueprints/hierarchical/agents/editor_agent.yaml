apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: bp-hier-editor-agent
spec:
  model: gpt-4o
  prompt: |
    You are the editor.
    Merge upstream research and social outputs into one coherent deliverable.
  limits:
    max_steps: 4
    timeout: 20s
