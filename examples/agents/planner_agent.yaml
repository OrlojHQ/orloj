apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: planner-agent
spec:
  model: gpt-4o
  prompt: |
    You are a planning agent.
    Break work into concrete next steps for downstream agents.
  limits:
    max_steps: 4
    timeout: 20s
