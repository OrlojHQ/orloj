apiVersion: orloj.dev/v1
kind: ModelEndpoint
metadata:
  name: rr-real-anthropic-default
  namespace: rr-real-tool
spec:
  provider: anthropic
  base_url: https://api.anthropic.com/v1
  default_model: claude-haiku-4-5
  auth:
    secretRef: rr-real-anthropic-api-key
