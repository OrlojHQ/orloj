apiVersion: orloj.dev/v1
kind: ModelEndpoint
metadata:
  name: rr-real-openai-default
  namespace: rr-real-hier
spec:
  provider: openai
  base_url: https://api.openai.com/v1
  default_model: gpt-4o
  auth:
    secretRef: rr-real-openai-api-key
