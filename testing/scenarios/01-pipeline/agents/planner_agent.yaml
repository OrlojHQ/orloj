apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: rt-pipeline-planner-agent
  namespace: rt-pipeline
spec:
  model: gpt-4o
  prompt: |
    You are the planning stage.
    Produce an actionable handoff for research.
  limits:
    max_steps: 4
    timeout: 20s
